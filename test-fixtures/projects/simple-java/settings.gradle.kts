// Test Fixture: simple-java
// Purpose: Healthy baseline project - should score 90+

rootProject.name = "simple-java"

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenCentral()
    }
}
