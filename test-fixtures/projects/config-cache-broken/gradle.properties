# Test Fixture: config-cache-broken
# Config cache NOT enabled (would fail)

org.gradle.parallel=true
org.gradle.caching=true
# org.gradle.configuration-cache=true  # Disabled - would fail
org.gradle.jvmargs=-Xmx2g
